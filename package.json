{
  "name": "ielts-90-day-mastery-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack (React + Express + MongoDB) 90-day IELTS study platform with 449 daily tasks.",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "npm start --prefix server",
    "seed": "npm run seed --prefix server",
    "verify": "npm run verify --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
