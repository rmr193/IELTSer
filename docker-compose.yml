# Optional: run a local MongoDB with `docker compose up -d`
services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - ielts_mongo_data:/data/db
volumes:
  ielts_mongo_data:
